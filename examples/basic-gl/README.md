# basic-gl

A very small and over-simplified OpenGL renderer for an Inochi2D model
written in C.

Requires C23, SDL3, GLEW and an OpenGL driver.

## Running
Build with `make` on Linux, then run from `out/` with `./in_glrender (model)`